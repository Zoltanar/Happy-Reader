<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:Happy_Reader.ViewModel"
    xmlns:tabs="clr-namespace:Happy_Reader.View.Tabs"
    x:Class="Happy_Reader.View.MainWindow"
    mc:Ignorable="d"
    Title="Happy Reader" MinWidth="800" MinHeight="600" Width="800" Height="600"  
    Loaded="Window_Loaded"
    Closing="MainWindow_OnClosing"
    Icon="/Happy Reader;component/Resources/logo-hr.png" d:DataContext="{d:DesignInstance viewModel:MainWindowViewModel}">
    <Window.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Padding="0,2,0,0" Background="LightBlue" Margin="-2,-2,0,0" Width="100" Height="50">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="AliceBlue" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Panel.ZIndex" Value="100"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="Black" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ContainerStyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <!-- ReSharper disable once Xaml.BindingWithContextNotResolved -->
                        <Expander Header="{Binding Name}" IsExpanded="True">
                            <ItemsPresenter />
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF2E4876">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0" x:Name="MainTabControl" Padding="0" TabStripPlacement="Left">
            <TabItem Header="Games" >
                <Grid Background="#FFC9E1F5" AllowDrop="True" Drop="DropFileOnGamesTab">
                    <ListBox x:Name="GameFiles"  Margin="10,10,10,53" MouseDoubleClick="GameFiles_MouseDoubleClick" ItemsSource="{Binding UserGameItems}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.GroupStyle>
                            <GroupStyle x:Name="UserGamesGroupStyle"  ContainerStyle="{StaticResource ContainerStyle}">
                            </GroupStyle>
                        </ListBox.GroupStyle>
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <Button Content="Debug"  Click="Debug_Button"  HorizontalAlignment="Left" Margin="10,0,0,10" Width="49" Height="38" VerticalAlignment="Bottom"/>
                    <Button Content="By Producer"  Click="GroupByProducer"  HorizontalAlignment="Left" Margin="192,0,0,11" Width="73" Height="38" VerticalAlignment="Bottom"/>
                    <Button Content="By Month"  Click="GroupByMonth"  HorizontalAlignment="Left" Margin="270,0,0,11" Width="73" Height="38" VerticalAlignment="Bottom"/>
                    <CheckBox Content="Capture Clipboard" HorizontalAlignment="Left" Margin="64,0,0,10" Width="123" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="38" VerticalAlignment="Bottom"  IsChecked="{Binding CaptureClipboard}"/>
                </Grid>
            </TabItem>
            <TabItem Header="ITHVNR" >
                <tabs:IthVnrTab DataContext="{Binding IthViewModel}"/>
            </TabItem>
            <TabItem Header="Entries">
                <Grid>
                    <CheckBox Content="Game-only" HorizontalAlignment="Left" VerticalAlignment="Top" IsChecked="{Binding OnlyGameEntries, Mode=TwoWay}"/>
                    <Button Content="Add Entry" Margin="83,0,0,0" VerticalAlignment="Top" Click="AddEntry_Click" HorizontalAlignment="Left" Width="90"/>
                    <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding EntriesList}" Margin="0,25,0,0" AutoGenerateColumns="False" CanUserAddRows="False" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                            <DataGridTextColumn Header="User" Binding="{Binding User}" />
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                            <DataGridTextColumn Header="Game" Binding="{Binding Game}" />
                            <DataGridTextColumn Header="Role" Binding="{Binding Role}" />
                            <DataGridTextColumn Header="Input" Binding="{Binding Input}" />
                            <DataGridTextColumn Header="Output" Binding="{Binding Output}" />
                            <DataGridTextColumn Header="Series-specific" Binding="{Binding SeriesSpecific}" />
                            <DataGridTextColumn Header="Regex" Binding="{Binding Regex}" />
                            <DataGridTemplateColumn Header="Delete">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Width="150" Content="Delete" Click="ClickDeleteButton" Background="CornflowerBlue"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Test">
                <tabs:TestTranslationTab DataContext="{Binding TestViewModel}"/>
            </TabItem>
            <TabItem Header="VNDB">
                <tabs:DatabaseTab DataContext="{Binding DatabaseViewModel}"/>
            </TabItem>
            <TabItem Header="Filters">
                <tabs:FiltersTab DataContext="{Binding FiltersViewModel}"/>
            </TabItem>
            <TabItem Header="Info/Settings">
                <Grid Background="DimGray">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Resources>
                                <Style TargetType="Control">
                                    <Setter Property="Foreground" Value="White" />
                                </Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type Control}}"/>
                                <Style TargetType="Label" BasedOn="{StaticResource {x:Type Control}}"/>
                            </Style.Resources>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="435"/>
                        </Grid.ColumnDefinitions>
                        <TextBox TextWrapping="Wrap" IsReadOnly="True" Text="Information About This Program" Margin="10,5" Background="Black" Foreground="White"/>
                        <GroupBox Grid.Column="1" Header="Settings" Foreground="White" BorderThickness="1" Margin="5">
                            <StackPanel>
                                <CheckBox Content="Show Content Tags" IsChecked="{Binding GSettings.ContentTags}"/>
                                <CheckBox Content="Show Sexual Tags" IsChecked="{Binding GSettings.SexualTags}"/>
                                <CheckBox Content="Show Technical Tags" IsChecked="{Binding GSettings.TechnicalTags}"/>
                                <CheckBox Content="Show NSFW Images" IsChecked="{Binding GSettings.NSFWImages}"/>
                                <CheckBox Content="Start Capturing Clipboard From Launch" IsChecked="{Binding GSettings.CaptureClipboardOnStart}"/>
                                <CheckBox Content="Pause IthVnr when pausing Translation" IsChecked="{Binding GSettings.PauseIthVnrAndTranslate}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="53"/>
                                        <ColumnDefinition Width="53"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Content="Max Clipboard Capture Size:"/>
                                    <Slider Grid.Column="1" Name="ClipboardSizeSlider" Value="{Binding GSettings.MaxClipboardSize, Mode=OneWay}" Minimum="0" Maximum="1000" Foreground="White" IsSnapToTickEnabled="True" Thumb.DragCompleted="SetClipboardSize"/>
                                    <TextBox Grid.Column="2" Text="{Binding ElementName=ClipboardSizeSlider, Path=Value}" Foreground="White" Style="{StaticResource NoBorderTextBox}"/>
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="57"/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="53"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Content="ITH Path:"/>
                                    <TextBox Grid.Column="1" x:Name="IthPathBox" Text="{Binding GSettings.IthPath}" Foreground="White" Style="{StaticResource NoBorderTextBox}"/>
                                    <Button Grid.Column="2" Content="Change" Click="ChangeIthPath"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                    </Grid>
                    <Grid Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2.5*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Height="47" Margin="10,10,10,0" TextWrapping="Wrap" Text="Send Query Here" VerticalAlignment="Top"/>
                        <Button Grid.Column="0" Content="Send" Margin="0,62,10,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="48"/>
                        <ListBox Grid.Column="0" Background="Black" ItemsSource="{Binding VndbQueries}" Margin="10,87,10,10" 
                                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Width="{Binding ViewportWidth, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ScrollViewer}}}">
                                        <TextBox TextWrapping="Wrap" Text="{Binding Path=DataContext ,RelativeSource={RelativeSource Self}}" Foreground="White" Background="{x:Null}" BorderBrush="LightGray" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Button Grid.Column="0" Content="Clear" Margin="0,62,63,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="45"/>
                        <CheckBox Foreground="White" Grid.Column="0" Content="Advanced Mode" Margin="0,65,113,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="71" IsChecked="{Binding GSettings.AdvancedMode}"/>
                        <ListBox Grid.Column="1" Margin="10" ItemsSource="{Binding VndbResponses}" Background="Black" 
                                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Width="{Binding ViewportWidth, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ScrollViewer}}}">
                                        <TextBox TextWrapping="Wrap" Text="{Binding Path=DataContext ,RelativeSource={RelativeSource Self}}" Foreground="White" Background="{x:Null}" BorderBrush="LightGray" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
        <Grid Grid.Row="1" Grid.Column="0" Background="LightSkyBlue" Height="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Foreground="Black" Content="{Binding DisplayUser}" Padding="5,0,25,0" HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>
            <Label Grid.Column="1" Foreground="Black" Content="{Binding DisplayGame}" Padding="5,0" HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>
            <Label Grid.Column="2" Foreground="Black" Content="{Binding StatusText}" Padding="0" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
